# ============================================
# Sistema LPLAN Central - Variáveis de ambiente
# PRODUÇÃO (ServHost/cPanel) - corrigido para CSRF/sessão
# ============================================

# --- Obrigatório ---
SECRET_KEY=django-insecure-lplan-central-v1-@!!-2026-segredo
DEBUG=False
ALLOWED_HOSTS=sistema.lplan.com.br,www.sistema.lplan.com.br,localhost,127.0.0.1

# --- CSRF / Sessão ---
CSRF_TRUSTED_ORIGINS=https://sistema.lplan.com.br,https://www.sistema.lplan.com.br,http://sistema.lplan.com.br,http://www.sistema.lplan.com.br

# --- Sessões ---
SESSION_ENGINE=django.contrib.sessions.backends.db

# --- HTTPS: True para cookies Secure e redirect HTTP→HTTPS (Django enxerga HTTPS via X-Forwarded-Proto) ---
SECURE_COOKIES_AND_REDIRECT=True

# --- Banco de dados (MySQL cPanel) ---
USE_MYSQL=True
DB_NAME=lplan_Sistema
DB_USER=lplan_gestaoap2
DB_PASSWORD=@#gestaoap
DB_HOST=localhost
DB_PORT=3306

# --- Celery / Redis ---
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# --- E-mail ---
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=mail.lplan.com.br
EMAIL_PORT=465
EMAIL_USE_SSL=True
EMAIL_USE_TLS=False
EMAIL_HOST_USER=sistema@lplan.com.br
EMAIL_HOST_PASSWORD=@#Sistema2026
DEFAULT_FROM_EMAIL=sistema@lplan.com.br
SITE_URL=https://sistema.lplan.com.br

# --- E-mail RDO (Relatório Diário de Obras) ---
# Quando definidos, os e-mails de diário de obra (para donos da obra e lista de destinatários) saem desta conta
EMAIL_RDO_FROM=rdo@lplan.com.br
EMAIL_RDO_HOST_USER=rdo@lplan.com.br
EMAIL_RDO_HOST_PASSWORD=@#RDO2026
# Opcional: se não definir, usa mesmo EMAIL_HOST/EMAIL_PORT/EMAIL_USE_SSL do bloco E-mail acima
# EMAIL_RDO_HOST=mail.lplan.com.br
# EMAIL_RDO_PORT=465
# EMAIL_RDO_USE_SSL=True

# --- Gestão de Aprovação ---
EMAIL_DEPARTAMENTOS_APROVACAO=departamento1@lplan.com.br,departamento2@lplan.com.br

# --- Sienge ---
SIENGE_API_BASE_URL=https://sua-url-sienge.com.br
SIENGE_API_CLIENT_ID=seu_client_id
SIENGE_API_CLIENT_SECRET=seu_client_secret
SIENGE_WEBHOOK_SECRET=seu_webhook_secret

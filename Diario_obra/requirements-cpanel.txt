# ============================================
# Sistema LPLAN Central - Dependências para cPanel
# ============================================
# Use este arquivo no servidor cPanel em vez de requirements.txt.
#
# Motivo: requirements.txt exige WeasyPrint>=60 e xhtml2pdf, que puxam
# pycairo. A compilação do pycairo precisa de "meson", e no cPanel
# ocorre: PermissionError: [Errno 13] Permission denied: 'meson'.
#
# Solução: WeasyPrint 52.x usa cairocffi (wheels pré-compilados, sem
# meson). No Linux/cPanel o PDF será gerado pelo WeasyPrint; xhtml2pdf
# não é instalado aqui para evitar a cadeia svglib → rlpycairo → pycairo.
# ============================================

# Core Django
Django>=5.0,<6.0
djangorestframework>=3.15.0

# Database
# cPanel: use apenas pymysql (mysqlclient exige compilar C e Python.h não está disponível)
psycopg2-binary>=2.9.0
pymysql>=1.1.0,<2.0.0

# Tree structure (EAP - Diario_obra)
django-treebeard>=4.7

# PDF: WeasyPrint 52.x (cairocffi, sem compilar pycairo). Não use >=60 no cPanel.
WeasyPrint>=52.5,<53
reportlab>=4.0.7,<5.0.0
html5lib>=1.1
PypDF2>=3.0.0

# Image processing
Pillow>=10.0.0,<11.0.0

# Task queue (Celery)
celery>=5.3.0
kombu>=5.3.0
billiard>=4.2.0
vine>=5.1.0

# Cache/Queue backend
redis>=5.0.0

# Utilities
python-dateutil>=2.8.2,<3.0.0
django-filter>=24.0
python-dotenv>=1.0.0,<2.0.0

# Excel export
openpyxl>=3.1.0,<4.0.0

# Data Processing (Mapa_Controle)
pandas>=2.0.0,<3.0.0

# HTTP Requests
requests>=2.31.0,<3.0.0

# Configuration & Environment
python-decouple>=3.8,<4.0.0
dj-database-url>=2.1.0,<3.0.0

# Static Files (produção)
whitenoise>=6.6.0,<7.0.0

# WeasyPrint 52.x (cairocffi + deps; evita pycairo)
cffi>=1.15.0
cairocffi>=1.4.0
cssselect2>=0.7.0
tinycss2>=1.2.0
pyphen>=0.14.0

# ============================================
# Sistema LPLAN Central - Variáveis de ambiente
# ============================================
# Copie para .env e preencha os valores.
# Em produção: nunca commite o arquivo .env no Git.

# --- Obrigatório em produção ---
SECRET_KEY=sua-chave-secreta-longa-e-aleatoria-aqui
DEBUG=False
# Produção cPanel (sistema.lplan.com.br):
ALLOWED_HOSTS=sistema.lplan.com.br
# Outros domínios: ALLOWED_HOSTS=sistema.lplan.com.br,www.sistema.lplan.com.br,outro.dominio.com

# --- Desenvolvimento local (use só na sua máquina) ---
# SECRET_KEY=dev-key-nao-usar-em-producao
# DEBUG=True
# ALLOWED_HOSTS=

# --- Banco de dados ---
# SQLite (padrão): não defina USE_MYSQL nem USE_POSTGRES.
#
# MySQL no cPanel (LPlan Sistema - mesmo usuário do GestControll):
USE_MYSQL=True
DB_NAME=lplan_Sistema
DB_USER=lplan_gestaoap2
DB_PASSWORD=COLOQUE_A_SENHA_AQUI
DB_HOST=localhost
DB_PORT=3306
#
# PostgreSQL (alternativa):
# USE_POSTGRES=True
# DB_NAME=lplan_central
# DB_USER=seu_usuario
# DB_PASSWORD=sua_senha
# DB_HOST=localhost
# DB_PORT=5432

# --- Celery / Redis ---
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# --- E-mail (recuperação de senha e notificações) ---
# Em produção use SMTP. Em dev o padrão envia para o console.
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.seudominio.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=sistema@lplan.com.br

# --- HTTPS (só quando DEBUG=False) ---
# Se o servidor tiver SSL, deixe True. Se ainda não tiver certificado, use False.
SECURE_COOKIES_AND_REDIRECT=True
